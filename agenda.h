#ifndef AGENDA_H
#define AGENDA_H

#include "corretor.h"
#include "imovel.h"
#include <vector>
#include <map>

void agendarVisitas(std::vector<Corretor>& corretores, const std::vector<Imovel>& imoveis);

#endif
